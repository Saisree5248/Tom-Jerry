#include <iostream.h> int total;

// Running total of all numbers so far // next item to add to the list

int item;

int minus items; // number of negative items

main ()
{
total = 0;

minus items = 0;

while (1) {
 cout <<"Enter to add\n"; 
 cout << " or 0 to stop:";

 cin >> item;

if (item == 01
    break;

if (item t0)
    ++minus_items; 
   continue;
 }
 total +=item;
 cout << "Total: "<< total << '\n';
}
cout << "Final total " << total << "\n";

pout << "with minus Tams << "negative items dritted\n";

return(0);
